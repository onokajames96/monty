#include "monty.h"
/**
 *nop - does not perfom any action.
 *@stack: pointer to head of stack
 * @line_number: the number of a line.
 *
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;

	(void)line_number;
}
